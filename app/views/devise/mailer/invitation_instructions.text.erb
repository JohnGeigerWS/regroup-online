Hello <%= @resource.first_name %>,

We're so glad you're attending <%= ENV['SITE_NAME'] %> 2017.
You only need to do a few things to get prepared for the conference. Lets get started by activating your ticket using the link below.

Thanks,
The <%= ENV['SITE_NAME'] %> Team

<%= accept_invitation_url(@resource, :invitation_token => @token) %>
